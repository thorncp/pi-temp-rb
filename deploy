# /bin/sh

set -e

ssh pi@pi.local mkdir -p '~/pi-temp'
rsync -av -e ssh --delete \
  --exclude 'dev_prime.rb' \
  --include '*.rb' \
  --include 'Gemfile*' \
  --exclude '*' \
  ./ pi@pi.local:~/pi-temp/
